services:
  postgres:
    image: postgres:16
    container_name: tg-postgres
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: thought_garden
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
